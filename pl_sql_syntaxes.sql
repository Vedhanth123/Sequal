-- 1) variables done
-- 2) if, else, switch case done
-- 3) loops done
-- 4) functions [return, argument] done, procedures [arguments]
-- 5) exceptions - exceptions are used to solve errors 

-- reverse a number
-- check the largest number in the group


-- if else syntax

-- if (condtion) then
--     statements;
-- endif;

-- if(condtion) then
--     statements;
-- else
--     statements;
-- endif;

-- if(condtion) then
--     statements;
-- elseif(condtion) then
--     statements
--     ...
--     ..
-- else
--     statements;
-- endif;

-- loop syntax

-- for(int i = 0 ; i < 5; i ++)  -- c language

-- for iterator in 10..20 loop

--     statements;

-- end loop;

-- while (condtion) loop

--     statements;

-- end loop;

-- function syntax

-- function function_name (var1 datatype, var2 datatype.....)
-- return datatype as
-- var3 datatype;

-- begin
--     statements;
-- end;

-- procedure

-- procedure procedure_name (var1 out datatype, var2 in datatype)
-- is

-- begin

--     statements;
-- end;

-- void main()


-- declare
-- -- declare variables to be used in the code

-- var datatype(size);


-- i = 0 -- example from c 
-- var datatype := 0;

-- begin

--     statements;

-- end;

